# ConfigPilot v1.0.11

本版本更新 ConfigPilot 的应用图标与仓库品牌资源。

## 主要更新

- 使用新的紫色 2048×2048 原图作为唯一应用图标源。
- 重新生成包含 16、24、32、48、64、128、256 像素尺寸的 Windows ICO。
- Windows 窗口、任务栏、安装程序和 macOS 构建均使用新图标源。
- README、应用截图和社交预览同步更新为新视觉资源。
- GitHub 仓库链接统一为规范名称 `aki-riko/ConfigPilot`。

## 下载

- Windows：`ConfigPilot_Setup_1.0.11.exe`
- macOS Apple Silicon：`ConfigPilot_1.0.11_arm64.dmg`

## macOS 首次安装须知

当前 macOS 版本仅支持 Apple Silicon（M 系列芯片），不支持 Intel Mac。

维护者目前没有 Apple Developer Program 会员，因此 DMG 尚未使用 Developer ID 签名和 Apple 公证。macOS Gatekeeper 可能显示“ConfigPilot 已损坏，无法打开”，但这不代表 DMG 文件真的损坏。

请先把 `ConfigPilot` 拖入 `Applications`，再打开“终端”执行：

```bash
xattr -dr com.apple.quarantine "/Applications/ConfigPilot.app"
open "/Applications/ConfigPilot.app"
```

同一已安装版本通常只需执行一次。请仅从本项目官方 Releases 下载。

## 验证

发布产物由 GitHub Actions 从 `v1.0.11` 标签构建。构建完成后补充产物大小和 SHA-256。
